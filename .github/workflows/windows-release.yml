name: windows-release

permissions:
  contents: write

on:
  push:
    branches:
      - main
      - master
    tags:
      - "v*"
  pull_request:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .[dev]

      - name: Run tests
        run: pytest

      - name: Build executable
        run: pyinstaller sparam_tool.spec --noconfirm

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: SParamAnalysisTool-windows
          path: dist/SParamAnalysisTool/**

      - name: Archive release payload
        if: startsWith(github.ref, 'refs/tags/v')
        run: |
          powershell Compress-Archive -Path dist/SParamAnalysisTool/* -DestinationPath dist/SParamAnalysisTool-${{ github.ref_name }}.zip

      - name: Publish GitHub Release asset
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: dist/SParamAnalysisTool-${{ github.ref_name }}.zip
